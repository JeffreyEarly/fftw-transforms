# Documentation

`WebsiteDocumentation` contains the hand-authored source for the FFTWTransforms
website. The package-specific `tools/build_website_documentation.m` hook copies
that source into `docs` and generates the version-history page from the root
`CHANGELOG.md`.

Edit the source tree and rebuild; do not edit generated pages in `docs` first.
